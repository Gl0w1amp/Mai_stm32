/*
 * LED.c
 *
 * Compatibility translation unit. LED implementation is split across
 * led_ws2812.c (WS2812 DMA driver), led_fet.c (FET PWM driver),
 * led_state.c (mode/render state machine + fade engine), and
 * mai2led.c (mai2 UART protocol + EEPROM/board-info).
 */
